function init() {
	console.log("hey it's matt!");
	// latlng = new google.maps.LatLng(...);
	// "..." is stuff you have to fill in

	// myOptions = {...};
	// map = new google.maps.Map(document.getElementById("map_canvas"), myOptions);
}